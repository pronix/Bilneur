<%= render :partial => 'dashboard/shared/quote_tabs', :locals => {:current => "Quote Options"} %>

    <div class="sptCntBus">
        <%= render 'shared/error_messages', :target => @address %>
        <div class="top"></div>
        <div class="cntr">
          <h2 class="htFix"></h2>
    <%= form_for(@quote, :url => dashboard_quote_path(@quote), :html => { :method => :put, :multipart => true, :class => "contInp contInpFix" }) do |f| %>
   
        <%= render :partial => 'form_options', :locals => {:f => f} %>

        <div class="bSaveC">
          <big>
            <button type="submit" class="btn otherbutton" id="save_shipping_method"></button>
          </big>
          <p>
            <%= t("or") %> <%= link_to t("actions.cancel"), dashboard_quotes_path %>
          </p>
        </div>
  
    <% end %>

  </div>
  <div class="btm"></div>
</div>
